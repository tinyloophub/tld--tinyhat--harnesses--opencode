# platform_repos/harnesses / opencode

A **future-option vendored harness**. Registered in `tinyhat_harness`
but NOT the active runtime — tinyhat v1 runs on the OpenAI Agents
SDK + sandbox composition (`agents-sdk-python`); OpenCode waits for
its per-harness runner adapter (v1.x). See PR #120 follow-up issues.

Pinned first-commit payload for the OpenCode Tinyhat harness mirror.

System repo:
`github.com/tinyloophub/tld--sys--tinyhat-harness-opencode`

Upstream:
`github.com/anomalyco/opencode` at `v1.14.28`
(`acd8783a36d8642ade7038f34ca4f2f2ac3cc824`)

Files in this directory are the Tinyhat-authored manifest,
compliance metadata, license, SBOM placeholder, and empty patch area.
The upstream source tree is mirrored in the managed system repo, not
in the main Tinyloop repository.
